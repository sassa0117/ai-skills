# アクスタ記事 v6 引き継ぎノート

## 今回（v6会話）でやったこと

### wp-sedori.md スキル修正（完了）
以下のSANGOブロックテンプレートが壊れていたので修正した：
- 見出し背景色: `whitesmoke` → `var(--sgb-pastel-color)`
- 画像ブロック: `{"id":0,"sizeSlug":"large","linkDestination":"none"}` + `src=""` + `class="wp-image-0"` → `<!-- wp:image -->` + `<img alt=""/>` のみ
- テーブルscopedCSS: コメント（`/* 右線 */` `/* 太さ調整 */` `/* 色調整 */` `/* 角丸調整 */`）追加。余分な`\n`も1箇所修正
- 「ブロック間の改行ルール」セクション新設（H2前3行、H3/H4前2行、吹き出し後1行、テーブル後1行）
- 裸の `<p><br></p>` 禁止ルール明記
- CSS省略禁止・構造簡略化禁止の警告を強化

**重要: AIはSANGOブロックを出力するたびにテンプレートを短縮・簡略化する癖がある。wp-sedori.mdの注意事項セクションに厳しく書いたが、毎回出力を検証すること。**

## 過去の会話（v5以前）で完了した修正

### 修正1: ヤフオク落札データ捏造 → 修正済み
- v2の正確な記述に修正：「オークファンで10年前の履歴が1件、未開封品4,000円」
- 値付けロジックもまどマギ類似事例ベースに書き直し済み

### 修正2: 化物語の有料パート解説 → v2の一次情報復元済み
- IP強度・限定性・希少構造・市場在庫・まどマギ類似事例の5ポイントを完全復元

### 修正4: やらかし集 → H4小見出し追加済み
### 修正5: 3つのズレ → H4で分割済み
### 修正6: 「アニメ文化が弱い地域」→ 表現修正済み
### 修正7: つなぎ文 → 書き直し済み

## 未完了（次のチャットでやること）

### 1. v4.htmlのSANGOブロック全修正（最優先）
v4.htmlは壊れたテンプレートから生成されたまま。wp-sedori.mdの修正後テンプレートで全ブロックを再生成する。

対象:
- **テーブル25個**: blockId連番→UUID、scopedCSSコメント追加、構造をテンプレートと完全一致
- **見出し**: `whitesmoke` → `var(--sgb-pastel-color)`（JSON + style両方）
- **画像**: 旧フォーマット → `<!-- wp:image -->` + `<img alt=""/>`
- **改行**: wp-sedori.mdの改行ルールに従って空パラグラフ挿入

注意:
- 一部テーブルのscopedCSSが複数行に展開されている（1行JSONではない）。両パターンに対応すること
- テキスト内容（記事本文）は変えるな。ブロック構造だけ直せ

### 2. WPアップロード → フロント検証
- wp-post.jsでアップロード
- フロントでテーブル表示確認（角丸・ボーダー・青ヘッダー列・白文字）

### 3. 画像プレースホルダー22箇所
- 位置が正しいか未検証

## 前のAIの誤診断（無視しろ）
「`<!-- /wp:table -->`の後ろにテンプレート内容がプレーンテキストでくっついているのが原因」→ **的外れ。実際の原因はテンプレート自体の破損。**

## v4の構成（確定。変えない）

### 無料部分
1. H2: アクスタで利益が出る仕組み — 5つの型
2. H2: 実例と有料部分のご案内（チラ見せ3件）
3. ペイウォール

### 有料部分
1. H2: 3つのフィルター
2. H2: 店舗でのアクスタ仕入れ
3. H2: 失敗談と値付け方法
4. H2: 5つの型 × 22件の実売データ
5. H2: まとめ

## 設計原則（変えない）
- What（何をすべきか）は無料。How（どうやるか）は有料
- 3つのフィルターの名前は無料で出す。使い方の詳細は有料
- 駿河屋の判断基準: 「買取価格×3でフリマ出品 → 売れなければ駿河屋買取に流す」（歪曲するな）
- 22件はリファレンス（最後に配置）
- 数字・データはv2の一次情報を正とする。勝手に変えるな

## 参照ファイル
- `output/acsta-article-full.html`: v2 HTML（一次情報の正確な記述）
- `output/acsta-article-v4.md`: v4本文（修正1-7反映済み）
- `output/acsta-article-v4.html`: 現行HTML（SANGOブロック修正必要）
- `wp-sedori.md`: **SANGOブロックの正しいテンプレート（v6で修正済み）**
- `wp-page-parts.md`: SANGO装飾パーツスキル

## WP下書き
- ID: 5388
- URL: https://sedorisassa.com/wp-admin/post.php?post=5388&action=edit
